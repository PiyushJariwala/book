class FireModel {
  String? name, image_link, author, about_author, about_book, rating, date;

  FireModel(
      {this.name,
      this.image_link,
      this.author,
      this.about_author,
      this.about_book,
      this.rating,
      this.date});
}
